auto-install-peers=true
strict-peer-dependencies=false
# Don't re-run `pnpm install` before every `pnpm <script>` — it purges/regenerates
# node_modules (breaking the generated Prisma client) and trips on unapproved build scripts.
verify-deps-before-run=false
